<div class="container">
  <%= render "shared/notice" %>
  <div class="card border-0 shadow shadow-sm">
    <div class="card-header border-0 d-flex justify-content-between">
      <span class="fs-4">Visualizar Funcao</span>
    </div>
    <div class="card-body">
      <div id="<%= dom_id @funcao %>">
          <p>
            <strong>Titulo:</strong>
            <%= @funcao.titulo %>
          </p>
          <p>
            <strong>Descricao:</strong>
            <%= @funcao.descricao %>
          </p>
      </div>
    </div>
    <div class="card-footer border-0 d-flex justify-content-between">
      <div>
        <%= link_to edit_funcao_path(@funcao), class: "btn btn-primary" do %>
        <i class="bi bi-pencil-square"></i> Editar
        <% end %>
        <%= link_to "Voltar", funcoes_path, class: "btn btn-secondary" %>
      </div>
      <%= button_to "Deletar", @funcao, method: :delete, class: "btn btn-outline-danger border-0" %>
    </div>
  </div>
</div>
